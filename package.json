{
  "name": "playground-spago",
  "version": "1.0.0",
  "description": "playground",
  "main": "index.js",
  "author": "nalkuatov",
  "license": "MIT",
  "scripts": {
    "serve": "yarn bundle && http-server dist",
    "bundle": "spago bundle-app --main Main --to dist/app.js && parcel build assets/index.html"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "purescript": "0.13.4"
  },
  "dependencies" : {
    "http-server": "^0.11.1"
  }
}
